let magic = function(arr1, arr2) {
    return arr1.concat(arr2);
};

// the same as 
let magic = (arr1, arr2) => {
    return arr1.concat(arr2);
};

// the same as 
let magic = (arr1, arr2) => arr1.concat(arr2);


console.log(magic([1, 2], [3, 4, 5]));






