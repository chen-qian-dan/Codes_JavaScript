
let val = ['Qian', 'Chen']; 

newVal = val.copyWithin(0, 2, 5)

console.log(newVal); 







/*
strVal = val.pop()
strVal = val.shift();
val.push('Dan'); // add item to the last.
val.pop('Dan'); // remove the last item
*/



















